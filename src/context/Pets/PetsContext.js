// ./src/context/Pets/PetsContext.js

/**
 * INSTANCIANDO CONTEXT - PERMITIR QUE TODOS LOS COMPONENTES PUEDAN RECIBIR DATOS DEL SOL (ESTADO GLOBAL)
 */

import { createContext } from 'react'

const PetsContext = createContext()

export default PetsContext
